fastapi>=0.115.0
uvicorn[standard]>=0.30.0
# Pinned, not floated: every deploy reinstalls dependencies, so a floating
# pin silently upgrades yt-dlp on deploys that had nothing to do with it.
# It was not the culprit for the 2026-09 suggestions outage, but ruling it
# out cost two isolated test runs that a pin would have made unnecessary.
# 2026.8.19 is what production was running when everything was verified.
yt-dlp==2026.8.19
pydantic>=2.10.0
httpx>=0.27.0
ytmusicapi>=1.11.0
bgutil-ytdlp-pot-provider
aiosqlite>=0.19.0
firebase-admin>=6.2.0
python-multipart>=0.0.6
PySocks>=1.7.1
# Cloudinary — image storage backend for profile avatars (Social v2
# R1) and one-time DM photos (R4). Firebase Storage requires the
# Blaze paid plan; Cloudinary's free tier (25 GB storage + 25 GB
# monthly bandwidth) covers our scale + needs no credit card.
# Server uses `cloudinary.utils.api_sign_request` to sign per-upload
# params so clients never see the API secret.
cloudinary>=1.39.0

# ── Postgres data layer (Phase 1: auth + users) ──
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.0
